<section class="catalog" id="catalog">
    <h2>Book Reviews</h2>
    <div class="container">
        {{#each book}}
            {{> book-card-catalog}}
        {{else}}
        <h2 class="no-record">There are no book review found yet...</h2>
        {{/each}}
    </div>
</section>